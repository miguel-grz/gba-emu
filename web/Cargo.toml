[package]
name = "gba-web"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "WebAssembly bindings for the GBA emulator core"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
gba-core = { path = "../core" }
wasm-bindgen = "0.2"
